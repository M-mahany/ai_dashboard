import { Button } from '@mui/material';

const ForgotPasswordButton = ({ data }) => {
  const handleSubmit = () => {};

  return (
    <Button className="submitBttn" onClick={handleSubmit}>
      Send Email
    </Button>
  );
};

export default ForgotPasswordButton;
